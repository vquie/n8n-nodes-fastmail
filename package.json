{
  "name": "n8n-nodes-fastmail",
  "version": "0.3.7",
  "description": "This Fastmail node for n8n is designed to set parameters and execute specific operations for the Fastmail API.",
  "keywords": [
    "n8n-community-node-package"
  ],
  "license": "MIT",
  "homepage": "https://github.com/vquie/n8n-nodes-fastmail#readme",
  "author": "Vitali Quiering <vitali@fastmask.app>",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/vquie/n8n-nodes-fastmail.git"
  },
  "main": "index.js",
  "scripts": {
    "build": "tsc && gulp build:icons",
    "build:dev": "tsc",
    "dev": "tsc --watch",
    "format": "prettier nodes credentials --write",
    "lint": "eslint nodes credentials package.json",
    "lintfix": "eslint nodes credentials package.json --fix",
    "prepack": "npm run build",
    "release:version": "node -e \"const fs=require('fs'); const pkg=require('./package.json'); const version=process.argv[1]; if(!version){console.error('Usage: npm run release:version -- <version>'); process.exit(1);} pkg.version=version; fs.writeFileSync('package.json', JSON.stringify(pkg, null, 2) + '\\n'); console.log('Updated package.json to', version);\"",
    "release:tag": "node -e \"const {execFileSync}=require('child_process'); const version=require('./package.json').version; const tag=`v${version}`; execFileSync('git',['add','package.json'],{stdio:'inherit'}); execFileSync('git',['commit','-m',`release: ${tag}`],{stdio:'inherit'}); execFileSync('git',['tag',tag],{stdio:'inherit'}); execFileSync('git',['push','origin'],{stdio:'inherit'}); execFileSync('git',['push','origin',tag],{stdio:'inherit'}); console.log('Pushed', tag);\""
  },
  "files": [
    "dist"
  ],
  "n8n": {
    "n8nNodesApiVersion": 1,
    "credentials": [
      "dist/credentials/FastmailApi.credentials.js",
      "dist/credentials/FastmailOAuth2Api.credentials.js"
    ],
    "nodes": [
      "dist/nodes/fastmail/Fastmail.node.js",
      "dist/nodes/fastmail/FastmailTrigger.node.js"
    ]
  },
  "devDependencies": {
    "@types/node": "^25.0.0",
    "@typescript-eslint/parser": "^8.25.0",
    "eslint": "^10.0.0",
    "eslint-plugin-n8n-nodes-base": "^1.16.4",
    "gulp": "^5.0.0",
    "gulp-cli": "^3.0.0",
    "n8n-workflow": "^2.4.0",
    "prettier": "^3.5.3",
    "typescript": "^5.8.2"
  }
}
